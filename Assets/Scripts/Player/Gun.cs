using UnityEngine;

public class Gun : MonoBehaviour
{
    // public GameObject bulletPrefab;  // پرِفَب گلوله
    // public Transform firePoint;      // نقطه‌ای که گلوله ازش خارج می‌شه
    // public float fireRate = 0.5f;    // مدت زمان بین هر شلیک
    // private float nextFireTime = 0f;

    // void Update()
    // {
    //     if (Input.GetMouseButton(0) && Time.time >= nextFireTime)
    //     {
    //         Shoot();
    //         nextFireTime = Time.time + fireRate;
    //     }
    // }

    // public void Shoot()
    // {
    //     Instantiate(Resources.Load("Prefabs/Bullet") as GameObject, firePoint.position, firePoint.rotation);
    //     // Instantiate(bulletPrefab, firePoint.position, firePoint.rotation);
    // }
}
